#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int maxProfit(vector<int>& prices) {
        if (prices.size() <= 1)
            return 0;

        if (prices.size() <= 4)
        {
            // code:  
        }

        return 0;
}

int main()
{
    std::cout << "For array [1]: " << maxProfit(vector<int>({1})) << std::endl;
    return 0;
}
